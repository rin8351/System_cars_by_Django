# Система управления автомобилями и запчастями

**Django-приложение для управления каталогом автомобилей, запчастей и автоматического расчета стоимости.**

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![Django Version](https://img.shields.io/badge/django-4.2+-green.svg)](https://www.djangoproject.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

---

## Описание проекта

Веб-приложение для управления автосервисом или автосалоном, которое позволяет:
- Вести каталог запчастей с ценами и характеристиками
- Управлять автомобилями и их комплектацией
- Автоматически рассчитывать стоимость автомобиля на основе запчастей и маржи
- Отслеживать связи между автомобилями и запчастями

### Основные возможности:

1. **Таблица запчастей (Parts)** — справочник деталей с указанием:
   - Модель запчасти
   - Тип (двигатель, коробка передач, кузов и т.д.)
   - Цена
   - Количество для одного автомобиля
   - Дополнительные параметры

2. **Таблица автомобилей (Cars)** — каталог автомобилей:
   - Название модели
   - Набор используемых запчастей
   - Маржа (наценка в процентах)
   - **Автоматический расчет итоговой стоимости** = (сумма стоимости запчастей) × (1 + маржа%)

3. **Таблица связей (Car Parts)** — автоматически заполняемая таблица, показывающая состав каждого автомобиля


4. **Управление профилем** — страница профиля пользователя с возможностью изменения пароля
   - Администратор создает профиль и начальный пароль для пользователя
   - Пользователь может изменить пароль на свой собственный


5. **Система прав доступа** — разделение прав доступа для разных ролей пользователей
   - Некоторые действия доступны только определенным группам пользователей
   - Контроль доступа к функциям редактирования и удаления


6. **Редактирование и удаление записей** — возможность изменения и удаления существующих записей запчастей и автомобилей


7. **Валидация данных** — автоматическая проверка корректности вводимых данных:
   - Проверка на ненулевые значения для цен, количества и маржи
   - Проверка на неотрицательные значения (запрет отрицательных чисел)

8. **Пагинация** — разбиение списков записей на страницы с ограничением в 15 записей на страницу


9. **Административная панель Django** —  доступна только администратору из меню сайта
---

### Поддержка баз данных

Проект поддерживает работу с двумя типами баз данных:
- **SQLite** — для быстрого старта и локальной разработки
- **PostgreSQL** — для production и изучения профессиональных инструментов

--

## Быстрый старт

### Вариант 1: Локальный запуск (рекомендуется для разработки)

#### Предварительные требования:
- Python 3.8 или выше
- pip
- virtualenv (рекомендуется)

#### Установка:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/System_cars_by_Django.git
cd System_cars_by_Django

# 2. Создайте виртуальное окружение
python -m venv venv

# 3. Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Перейдите в директорию проекта
cd cars

# 6. Примените миграции базы данных
python manage.py migrate

# 7. Создайте суперпользователя для админ-панели
python manage.py createsuperuser

# 8. (Опционально) Загрузите демо-данные
python manage.py load_demo_data

# 9. Запустите сервер разработки
python manage.py runserver
```

**Откройте в браузере:** http://127.0.0.1:8000/

#### Демо-данные

Команда `load_demo_data` создает готовый набор тестовых данных для быстрого ознакомления с системой:
В таблицах заполняются все поля, кроме столбца "Автор", то есть имя юзера, создавшего эту запись. Оно может быть пустым. В дальнейшем заполняется автоматически в зависимости от того залогинен ли пользователь.

```bash
python manage.py load_demo_data
```

Эта команда создаст:
- ✅ 21 запчасть (двигатели, коробки передач, кузова, колеса, салоны и т.д.)
- ✅ 5 автомобилей с различными комплектациями
- ✅ Все необходимые связи между автомобилями и запчастями

Полезные опции:
```bash
# Очистить существующие данные и загрузить заново
python manage.py load_demo_data --clear
```

---

### Вариант 1.1: Запуск с PostgreSQL 🐘

Если вы хотите использовать PostgreSQL вместо SQLite:

#### Windows:
```bash
# 1. Установите PostgreSQL (если еще не установлен)
# Скачайте с https://www.postgresql.org/download/windows/

# 2. Создайте файл .env из шаблона
copy env_template.txt .env
# Отредактируйте .env и замените пароль

# 3. Сделайте настройку

```bash
# 1. Создайте базу данных (в psql или pgAdmin)
# CREATE DATABASE cars_db;
# CREATE USER cars_user WITH PASSWORD 'your_password';
# GRANT ALL PRIVILEGES ON DATABASE cars_db TO cars_user;

# 2. Создайте .env файл
USE_POSTGRES=True
DB_NAME=cars_db
DB_USER=cars_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Примените миграции
python manage.py migrate

# 5. Запустите сервер
python manage.py runserver

---

### Вариант 2: Запуск с Docker

#### Предварительные требования:
- Docker
- Docker Compose

#### Установка и запуск:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/System_cars_by_Django.git
cd System_cars_by_Django

# 2. Запустите контейнеры
docker-compose up --build

# 3. В отдельном терминале создайте суперпользователя
docker-compose exec web python manage.py createsuperuser
```

Приложение будет доступно по адресу: **http://localhost:8000/**

---

## 📖 Использование

### Основные URL-адреса:

| URL | Описание |
|-----|----------|
| `/` | Главная страница (требует авторизации) |
| `/register/` | Регистрация нового пользователя |
| `/login/` | Вход в систему |
| `/logout/` | Выход из системы |
| `/profile/` | Страница профиля пользователя |
| `/change-password/` | Изменение пароля пользователя |
| `/parts/` | Список всех запчастей (с пагинацией) |
| `/cars/` | Список всех автомобилей (с пагинацией) |
| `/addparts/` | Добавить новую запчасть |
| `/addcars/` | Добавить новый автомобиль |
| `/editparts/<id>/` | Редактировать запчасть |
| `/editcars/<id>/` | Редактировать автомобиль |
| `/deleteparts/<id>/` | Удалить запчасть |
| `/deletecars/<id>/` | Удалить автомобиль |
| `/acessor/` | Таблица связей автомобилей и запчастей |
| `/admin/` | Административная панель Django |

### Рабочий процесс:

1. **Зарегистрируйтесь** или войдите в систему
2. **Управление профилем**: 
   - Администратор создает профиль пользователя с начальным паролем
   - Пользователь может изменить пароль через `/change-password/`
   - Просмотр и управление профилем доступны на странице `/profile/`
3. **Добавьте запчасти** через `/addparts/` или админ-панель
   - При добавлении система автоматически проверяет корректность данных (цены, количество, маржа должны быть положительными числами)
4. **Создайте автомобиль** через `/addcars/`, выбрав нужные запчасти и указав маржу
   - Валидация данных гарантирует корректность введенных значений
5. **Система автоматически рассчитает** итоговую стоимость автомобиля
6. **Просмотрите результаты** в таблицах `/cars/` и `/acessor/` (списки разбиты на страницы по 15 записей)
7. **Редактирование и удаление**: 
   - Редактируйте существующие записи через `/editparts/<id>/` и `/editcars/<id>/`
   - Удаляйте записи через `/deleteparts/<id>/` и `/deletecars/<id>/`
   - Доступ к функциям редактирования и удаления зависит от прав пользователя

---

## Конфигурация

**На данынй момент используется PostgreSQL** в файле env стоит USE_POSTGRES=True. 
Если потребуется вернуться в SQLite достаточно установить USE_POSTGRES=False
---

## Структура проекта

```
System_cars_by_Django/
├── cars/                      # Основная директория Django-проекта
│   ├── cars/                  # Настройки проекта
│   │   ├── settings.py        # Конфигурация Django
│   │   ├── urls.py            # Главный URL-роутинг
│   │   └── wsgi.py            # WSGI-конфигурация
│   ├── carsdb/                # Приложение Django
│   │   ├── models.py          # Модели данных (Parts, Cars, CarPart)
│   │   ├── views.py           # Представления (логика обработки запросов)
│   │   ├── forms.py           # Формы для создания объектов
│   │   ├── admin.py           # Настройки админ-панели
│   │   ├── urls.py            # URL-маршруты приложения
│   │   ├── templates/         # HTML-шаблоны
│   │   └── static/            # CSS, JS, изображения
│   └── manage.py              # Утилита управления Django
├── docker-compose.yml         # Конфигурация Docker Compose
├── Dockerfile                 # Инструкции для сборки Docker-образа
├── requirements.txt           # Зависимости Python
└── README.md                  # Этот файл
```


## Администрирование

### Доступ к админ-панели:

1. Создайте суперпользователя (если еще не создали):
   ```bash
   python manage.py createsuperuser
   ```

2. Перейдите по адресу: **http://127.0.0.1:8000/admin/**

3. Войдите с учетными данными суперпользователя

В админ-панели вы можете:
- Управлять пользователями и создавать для них профили с начальными паролями
- Настраивать права доступа для разных групп пользователей
- Добавлять/редактировать/удалять запчасти
- Управлять автомобилями
- Просматривать связи между автомобилями и запчастями

### Полезные команды управления:

```bash
# Создать нового суперпользователя
python manage.py createsuperuser

# Загрузить демо-данные для тестирования
python manage.py load_demo_data

# Очистить и загрузить демо-данные заново
python manage.py load_demo_data --clear

# Создать новые миграции (после изменения models.py)
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Собрать статические файлы (для production)
python manage.py collectstatic
```

---
